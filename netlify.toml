[build]
  base    = "frontend"
  publish = "build"          # relative to base
  command = "npm run build"

[build.environment]
  CI = "false"

[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200